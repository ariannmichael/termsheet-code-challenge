class DealFilter {
 type: string;
 purchasePrice: number;

 constructor(type: string, purchasePrice: number) {
  this.type = type;
  this.purchasePrice = purchasePrice;
 }
}

export default DealFilter;